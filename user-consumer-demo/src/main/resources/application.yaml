server:
  port: 8845

spring:
  application:
    name: user-consumer-demo
  cloud:
    # 配置nacos注册中心
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # 注册中心地址
        cluster-name: BJ # 集群名称
